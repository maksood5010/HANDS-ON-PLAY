# Included by nginx (e.g. /etc/nginx/conf.d/). Raises proxy body limit for playlist uploads.
# Must be >= largest single file you allow (see PLAYLIST_UPLOAD_MAX_MB on the API).
client_max_body_size 500m;
